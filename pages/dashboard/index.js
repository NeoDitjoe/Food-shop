import AddToMenu from "@/Dashboard/addToMenu";
import { useRef } from "react";

export default function Dashboard(){

    // const inputRef = useRef()

    // function inputs(){
    //     const inputValue = inputRef.current.value

    //     return inputValue
    // }

    return(
        <AddToMenu /* 
            submit={() => alert(inputs())}
            inputRef={inputRef} */
        />
    )
}